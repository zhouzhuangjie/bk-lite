[[inputs.consul]]
    address = "{{ url }}"
    interval = "{{ interval }}s"
    [inputs.consul.tags]
        instance_id = "{{ instance_id }}"
        instance_type = "{{ instance_type }}"
        collect_type = "middleware"
        config_type = "consul"