[[inputs.net]]
    interval = "{{ interval }}s"
    [inputs.net.tags]
        instance_id = "{{ instance_id }}"
        instance_type = "{{ instance_type }}"
        collect_type = "host"
        config_type = "net"